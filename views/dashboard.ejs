<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - MiniClick</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen p-6">
  <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-lg">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold text-gray-800">Your MiniClick Dashboard</h1>
      <div>
        <a href="/" class="text-blue-600 hover:underline mr-4">Home</a>
        <a href="/logout" class="text-blue-600 hover:underline">Logout</a>
      </div>
    </div>

    
    <% if (!isPro) { %>
      <div class="bg-yellow-100 p-4 rounded mb-6 text-center">
        <p class="text-yellow-800">Free Tier: 5 links/month. 
          <a href="#" class="text-blue-600 hover:underline">Upgrade to Pro ($2/month)</a>
        </p>
      </div>
    <% } else { %>
      <div class="bg-green-100 p-4 rounded mb-6 text-center">
        <p class="text-green-800">Pro Tier: Unlimited links!</p>
      </div>
    <% } %>
    <h2 class="text-xl font-semibold text-gray-700 mb-4">Your Links</h2>
    <div class="overflow-x-auto">
      <table class="w-full text-left border-collapse">
        <thead>
          <tr class="bg-gray-200">
            <th class="p-3 font-semibold text-gray-700">Short URL</th>
            <th class="p-3 font-semibold text-gray-700">Original URL</th>
            <th class="p-3 font-semibold text-gray-700">Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% links.forEach(link => { %>
            <tr class="border-b hover:bg-gray-50">
              <td class="p-3">
                <a href="/<%= link.short_code %>" class="text-blue-600 hover:underline">
                  <%= link.short_code %>
                </a>
              </td>
              <td class="p-3 text-gray-600 truncate max-w-xs"><%= link.original_url %></td>
              <td class="p-3 text-gray-600"><%= link.clicks %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>